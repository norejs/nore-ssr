{
    "name": "root",
    "private": true,
    "scripts": {
        "start": "lerna run start --stream --loglevel verbose"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "dependencies": {
        "@emotion/css": "^11.11.2",
        "@emotion/server": "^11.11.0"
    }
}
